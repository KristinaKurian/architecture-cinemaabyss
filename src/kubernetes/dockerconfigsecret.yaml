apiVersion: v1
kind: Secret
metadata:
  name: dockerconfigjson
  namespace: cinemaabyss
type: kubernetes.io/dockerconfigjson
data:
  # Безопасная заглушка: base64 от {"auths":{}}.
  # Для приватных GHCR-образов замените base64 из ~/.docker/config.json.
  .dockerconfigjson: 
